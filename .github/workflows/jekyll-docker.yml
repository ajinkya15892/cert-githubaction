name: First workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: test-linux-ubuntu
    steps:
      - name: Print Greeting
        run: |
             echo "Hello World!"
             echo "Multi command"
      - name: Print GoodBye
        run: echo "Done - Bye!"
      
